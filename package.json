{
	"name": "chat",
	"private": true,
	"scripts": {
		"start": "nodemon --exec 'node .'",
		"now-start": "NODE_ENV=production node .",
		"format": "prettier --write --use-tabs --single-quote --no-semi --no-bracket-spacing 'index.js' 'public/**/*.js'"
	},
	"dependencies": {
		"express": "^4.15.2",
		"nodemon": "^1.11.0",
		"socket.io": "^2.0.1"
	},
	"devDependencies": {
		"prettier": "^1.3.1"
	}
}
